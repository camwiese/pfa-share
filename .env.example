NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000
GP_EMAIL=camwiese@gmail.com
RESEND_API_KEY=
AUTH_SENDER_EMAIL=hello@worldsfair.co
SESSION_SECRET=
CRON_SECRET=

# Local dev only — auto-login as the first GP_EMAIL.
LOCAL_DEV_ADMIN_BYPASS=true

# Skip real OTP delivery — any 6-digit code is accepted on both /admin
# sign-in and the visitor gate. Safe to leave on while wiring email
# delivery; flip to false once Resend SMTP is live in Supabase.
AUTH_DUMMY_MODE=false
