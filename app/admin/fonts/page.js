import FontComparison from "../../../components/admin/FontComparison";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Font Preview — Letter Slide",
};

export default function FontsPage() {
  return <FontComparison />;
}
